# 加密管理

## 概述

本文件记录 CoinScape 项目中关于 WebDAV 密码与登录认证的加密、存储、迁移与使用细则。目的是保证敏感信息在磁盘上不明文保存，并在 Web 客户端下通过后端代理安全地进行同步操作，同时提供账户登录功能以控制对软件的访问。

---

## 目标

- `sync.webdav.password` 在磁盘（`backend/data/app_settings.json`）中必须以不可读的加密格式保存。
- 设置界面中密码输入框不预填明文，显示为 `*`（即留空并仅在用户输入新密码时提交）。
- 后端能在需要时（运行时内存中）解密密码以进行 WebDAV 操作。
- 增加简单的账户登录机制：用户名+密码验证，密码以不可逆哈希存储（PBKDF2）；初始默认账号 `admin`/`coinscape`。
- 提供迁移策略：对已有明文 password 的旧 `app_settings.json` 自动加密并覆盖保存（第一次读取时迁移）。

---

## 设计与实现细节

1. 对称加密（WebDAV 密码）
   - 算法：AES-256-GCM（认证加密）。
   - 密钥来源：优先使用环境变量 `COINSCAPE_SECRET_KEY`（推荐以 base64 形式提供 32 字节密钥）；若未设置，则在首次运行时在 `backend/data/secret.key` 中生成并保存一个随机 32 字节密钥（以 base64 存储）。
   - 密钥派生：为每次加密使用一个随机 salt（16 字节），用 PBKDF2-HMAC-SHA256（100000 轮）以 `COINSCAPE_SECRET_KEY`（字节）和该 salt 派生 AES 密钥（32 字节），然后用 AES-GCM（12 字节 nonce）加密。
   - 存储格式：`enc:<salt_b64>:<nonce_b64>:<ciphertext_b64>`（以字符串形式写入 `app_settings.json`）。

2. 账户密码（登录）
   - 算法：PBKDF2-HMAC-SHA256，迭代 200000 次，salt 长度 16 字节。
   - 存储格式：`<salt_b64>:<hash_hex>`，保存在 `app_settings.json` 的 `auth.password_hash` 字段。
   - 初始账号：`username: admin`，`password: coinscape`（会在首次运行时以 PBKDF2 哈希写入 `app_settings.json`）。

3. API 行为
   - `GET /api/settings`：返回 `app_settings.json`（其中 WebDAV 密码以加密字符串形式存在）。后端不返回明文密码。
   - `PUT /api/settings`：用于更新设置。若 payload 中包含 `sync.webdav.password` 且非空，则后端会在写盘前将明文加密并保存；若该字段缺失或为空，则后端保持原有加密密码不变（前端应在未修改密码时不提交空值，以避免覆盖）。
   - `POST /api/auth/login`：接收 `{"username","password"}`；后端对比 PBKDF2 校验，成功返回 `200` 和 `{"success": true}`，失败返回 `401`。

4. 前端行为
   - 设置页密码输入框不预填（初始留空），仅在用户确实输入新密码并保存时将明文发送给 `PUT /api/settings`（Web 端会发送，后端负责加密并写盘）。
   - 当用户切换“开启后端代理”时，仅更新 `backend.proxy_enabled`（并可立即保存），是否生效由服务端重启或路由重新注册决定（可按需选择立即生效或需重启）。

5. 迁移策略
   - 如果 `app_settings.json` 中存在未加密的 `sync.webdav.password`（字符串且不以 `enc:` 开头），后端在首次读取时自动加密该字段并保存回文件。

6. 文件与代码修改清单
   - `backend/requirements.txt`：新增 `cryptography`。
   - 新增 `backend/crypto.py`：包含密钥管理、AES-GCM 加密/解密、账户密码哈希与校验功能。
   - 修改 `backend/database.py`：在 `load_app_settings` 中执行明文密码迁移；在 `update_app_settings` 中对传入的 `sync.webdav.password` 进行加密处理；在默认设置中加入 `auth` 段（若不存在则生成初始账号的哈希）。
   - 修改 `backend/main.py`：新增 `POST /api/auth/login` 认证接口（使用 `database` 中的 `auth` 配置进行校验）。
   - 修改 `lib/screens/settings_screen.dart`：密码输入框不预填，保存时仅在输入非空密码时提交；显示为 `*`。
   - 修改 `lib/providers/sync_providers.dart`：Web 环境下保存 WebDAV 配置时（若提供密码）向后端提交明文，后端负责加密并写盘。

7. 测试步骤
   1. 启动后端服务，确认 `backend/data/secret.key`（若未设置 env）已生成，且 `app_settings.json` 中 `sync.webdav.password` 已被加密（`enc:` 前缀）。
   2. 访问设置界面，确认密码输入框为空（不显示明文），修改并保存新密码后检查 `app_settings.json` 中对应字段为 `enc:...`。
   3. 尝试 `POST /api/auth/login` 使用 `admin`/`coinscape` 登录，返回成功；修改账户密码后确认旧口令失效、使用新口令能登录。
   4. 在 Web 环境下启用后端代理，执行 Push/Pull 操作，后端应能解密并成功与 WebDAV 通信。

8. 运维注意事项
   - 强烈建议在生产环境通过 `COINSCAPE_SECRET_KEY` 明确注入密钥（以 base64 形式），避免密钥文件分发风险。
   - 旋转 `COINSCAPE_SECRET_KEY` 会导致已加密的密码无法解密——如果需要支持密钥旋转，需实现密钥版本管理与重加密流程（不在本次实现范围内）。
   - `backend/data/secret.key` 文件权限应限制为仅服务运行用户可读写（Unix 上 chmod 600）。

---

如上方案确认后我将开始依次实现以上改动，并在每一步完成后提供简短说明与自测建议。
